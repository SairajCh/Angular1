import { RenderCustomizedstyledirectiveDirective } from './render-customizedstyledirective.directive';

describe('RenderCustomizedstyledirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderCustomizedstyledirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
